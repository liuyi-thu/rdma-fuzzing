一个问题是 RDMA CM 是以 event 为中心的，需要对 event 进行及时处理？
TODO: rdma_get_cm_event 和 rdma_ack_cm_event 可能需要附着一下？放在哪里

rdma_create_event_channel -- reviewed
rdma_create_id -- context_ptr 可能有问题 -- reviewed
rdma_resolve_addr -- cm_id 的状态到底需要修改吗？ -- reviewed -- sockaddr 的状态可能得想一下，其实是constant
rdma_resolve_route -- 能否解决重复变量问题？ -- reviewed
rdma_create_qp -- 本质上调用 ibv_create_qp(_ex)，可能需要删除 socket 建链的内容 -- reviewed
rdma_connect -- 没有对 conn_param_obj 建模 -- reviewed (not tested yet)

rdma_disconnect -- reviewed
rdma_destroy_qp -- 没有解决 qp binding 问题
rdma_destroy_id
rdma_destroy_event_channel

我认为 CM 最好列为一个单独的模式，和 socket 建链独立起来，不过论文里不一定需要这么说
引入部分 IB verbs

是否能纯粹用 CM API 完成一次通信？我认为是可以的
比如 rdma_post_send 的用法
混搭当然是可以的